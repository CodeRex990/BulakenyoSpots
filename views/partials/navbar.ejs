<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <!-- Brand and Toggler -->
    <a class="navbar-brand d-flex align-items-center fs-2 text-black fw-bolder" href="#">
      <img src="/src/bsl.png" alt="BulakanSpotted" width="40" height="40" class="me-2"> BULAKENYO Spotted
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar Links -->
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <!-- Left Links -->
      <div class="navbar-nav">
        <a class="nav-link fw-bolder fs-5 <%= currentPath === '/' ? 'active' : '' %>" href="/">HOME</a>
        <a class="nav-link fw-bolder fs-5 <%= currentPath === '/spotgrounds' ? 'active' : '' %>" href="/spotgrounds">SPOTS</a>
      </div>
      
      <!-- Right Links -->
      <div class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
          <a class="nav-link fw-bolder fs-5 <%= currentPath === '/login' ? 'active' : '' %>" href="/login">LOGIN</a>
          <a class="nav-link fw-bolder fs-5 <%= currentPath === '/register' ? 'active' : '' %>" href="/register">REGISTER</a>
        <% } else { %>
          <a class="nav-link fw-bolder fs-5" href="/logout">LOGOUT</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>

<style>
  /* General Navbar Styling */
  .nav-link {
    color: black;
    margin: 5px;
    border-radius: 10px;
    letter-spacing: 2px;
    transition: all 0.3s ease-in-out;
  }

  .nav-link:hover {
    background-color: #63806f;
    color: white;
  }

  .nav-link.active {
    background-color: #63806f;
    color: white;
    font-weight: bold;
  }

  .navbar-brand img {
    border-radius: 5px;
  }

  @media (max-width: 991px) {
    /* Adjust navbar styling for smaller screens */
    .nav-link {
      font-size: 1rem;
      text-align: center;
      margin: 10px 0;
    }
  }
</style>
